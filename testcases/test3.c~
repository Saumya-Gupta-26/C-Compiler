/* if-else demonstrated via finding maximum of 3 numbers */
void main()
{
	int a, b, c;
	a=8;
	b=9;
	c=3;
	
	int max;

	if(a<b)
	{
		max=b;
	
		if(max<c)
		{
			max=c;
		}
	}	
	
	else
	{
		max=a;
	
		if(max<c)
		{
			max=c;
		}
	}

	printf("\nMaximum of %d, %d, and, %d is %d\n", a, b, c, max);
}

